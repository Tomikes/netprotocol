lan:局域网
wan:广域网
internet:内网

数据包:数据从网络线路分块传输，每块数据叫做数据包。

网络互联模型：物理层，数据链路层，网络层，传输层，会话层，表示层。应用层

应用层：提供用户界面和借口（http。smtp，ftp。telnet）
表示层：表示数据，进行加密压缩等处理（ascii,mpeg）
会话层：将不通的应用程序数据分离（sap,sdp）
传输层：提供可靠和不可靠的数据传输，重传，纠错（tcp，udp）
网络层：提供逻辑地址，路由器用他们来选择路径
数据链路层：将分组拆分成字节，合并成数据帧
物理层：在设备之间传输比特流
